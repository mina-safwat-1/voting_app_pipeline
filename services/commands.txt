# command for result service
docker container run -p 80:80 -e DB_USER=user -e DB_PASSWORD=pass -e DB_HOST=host  -e DB_NAME=postgres  result

# command for voting service
docker container run -p 80:80 -e REDIS_HOST=host  vote

# command for worker service
docker container run -p 80:80 -e DB_USER=user -e DB_PASSWORD=pass -e DB_HOST=host  -e REDIS_HOST=host  worker
